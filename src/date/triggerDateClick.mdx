---
menu: Date
name: triggerDateClick 获取焦点
route: /date/triggerDateClick
---
import '../docz.less'

# triggerDateClick
触发 DatePicker 组件的 Click 事件，以弹出日期选择浮层，并获得输入焦点

### 参数

```typescript
/**
* 触发 DatePicker 组件的 Click 事件，以弹出日期选择浮层，并获得输入焦点
* @param e Event
*
*/
const triggerDateClick = (e) => e;
```

### 实例

```javascript

<DatePicker Click={(e)=>{
    triggerDateClick(e)
}}/>

/**
*获得输入焦点
*
*/

```