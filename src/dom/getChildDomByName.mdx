---
menu: Dom
name: getChildDomByName 获取节点
route: /dom/getChildDomByName
---
import '../docz.less'

# getChildDomByName
获取 dom 节点 $container 的 tagName = childName 的子节点

### 参数

```typescript
/**
 * 获取 dom 节点 $container 的 tagName = childName 的子节点
 * @param $container DomElement
 * @param childTagName String 子节点的 tagName
 * @returns {null}
 */
const  getChildDomByName = ($container, childTagName) => {}
```

