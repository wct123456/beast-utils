---
menu: Utils
name: setStorage 设置缓存
route: /utils/setStorage
---
import '../docz.less'

# setStorage
设置缓存，保存在浏览器，方便下次调用

### 参数

```typescript
/**
 * 生成随机数
 * @param key <string>
 * @param val <any>
 */
const setStorage = (key, val) =>null;
```

### 实例

```javascript
let val = '保存，下次使用'
setStorage('save',val);

/**
* 无输出
*
*
*/

```