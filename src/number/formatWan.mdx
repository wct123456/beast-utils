---
menu: Number
name: formatWan 金额以万为单位
route: /number/formatWan
---
import '../docz.less'

# formatWan
把金额大于 10000 时 ，转成 以‘万’为单位的DOM元素,并向下取“整”万

### 参数

```typescript
/**
* 把金额转成 以万为单位
* @param val<string|number>
* @return DOM元素
*/
const formatWan = (val) => DOM;
```

### 实例

```javascript
let val  = 566000
let money = formatWan(val);

/**
* 输出：56万
*
*/

```